import 'package:flutter/material.dart';

class ServDRHistoryScreen extends StatelessWidget {
  const ServDRHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('ServDR History'),
      ),
      body: Center(
        child: Text('ServDR History Screen'),
      ),
    );
  }
}
